import React, {useState,useEffect} from 'react';

function IPAddressInformation({versionOfIPAddress}){
    return (
     const [IPAddress,setIPAddress]=useState('');
     const ipifyURL=`https://api.ipify.org?format=json&${versionOfIPAddress === 'ipv6' ? 'ipv6' : ''}`;"
    );
}

export default IPAddressInformation;